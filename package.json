{
  "name": "memo-mcp",
  "version": "0.1.0",
  "description": "MCP server for attaching on-chain memos to Base B20 payments ($MEMO). Lets AI agents send and read memos via transferWithMemo.",
  "type": "module",
  "bin": {
    "memo-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "viem": "^2.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
