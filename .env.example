# Base RPC endpoint
RPC_URL=https://mainnet.base.org

# The B20 token this server operates on (your $MEMO address, 0xb200...)
MEMO_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Signer key — ONLY needed for send_memo. Leave unset for read-only use.
# MEMO_PRIVATE_KEY=0x...

# Optional: default start block for read_memos caller scans (e.g. the $MEMO deploy block)
# MEMO_FROM_BLOCK=
